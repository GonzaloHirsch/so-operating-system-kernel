REQUISITOS PREVIOS:
*Tener instalado los siguientes programas en su sistema operativo (Tipo Unix):
	- qemu 
	- docker
*Registrarce en docker.

Se debe contener en la carpeta principal del trabajo práctico especial los archivos:

	- Dockerfile
	- docker.sh

Se debe ejecutar desde la terminal sobre el directorio de trabajo:
	
	./docker.sh nombreUsuarioDocker

Esto compila y linkedita su trabajo y luego basta correr

	./run.sh

Para poder correr su trabajo.
